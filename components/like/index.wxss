.heart-wrap{
  display: inline-flex;
}
.heart-wrap .heart{
  /* rpx是屏幕适配的像素 */
  /* px是固定像素 */
  /* 根据需求选择自己想要的像素单位 */
  width: 32rpx;
  height: 28rpx;
}
.heart-wrap text{
  font-size: 24rpx;
  position: relative;
  bottom: 15rpx;
}